<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= title || 'My Wishlist' %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 2rem; }
      .wrap { max-width: 800px; margin: 0 auto; }
      h1 { margin-bottom: .5rem; text-align:center; }
      h2 { margin-bottom: .5rem; text-align:center; }
      p.help { color:#555; text-align:center; }
      .actions { margin: 1.25rem 0 2rem; text-align:center; }
      a.btn { display:inline-block; padding:.5rem .9rem; border:1px solid #bbb; border-radius:.5rem; text-decoration:none; color:#222; background:#f7f7f7; }
      a.btn:hover { background:#eee; }
      .empty { text-align:center; color:#444; font-style:italic; }
    </style>
  </head>
  <body>
    <div class="wrap">
      <h1><%= title || 'My Wishlist' %></h1>
      <p class="help">Halaman wishlist aktif. Fungsinya akan diisi nanti (sesuai prioritas tim).</p>

      <% if (data.Products.length) { %>
        <h2>Hello, <%= data.Profile.fullName %>, wanna checkout now?</h2>
      <table border="1">
        <tr>
          <th>No.</th>
          <th>Product Name</th>
          <th>Category</th>
          <th>Description</th>
          <th>Price</th>
          <th>Last Updated</th>
          <th>Contact</th>
          <th>Address</th>
          <th>Action</th>
        </tr>
        <% data.Products.forEach((el, i) => { %>
          <tr>
            <td><%= i + 1 %></td>
            <td><%= el.name %></td>
            <td><%= el.Category.name %></td>
            <td><%= el.description %></td>
            <td><%= el.idr %></td>
            <td><%= el.lastUpdate(el.updatedAt) %></td>
            <td><%= el.User.Profile.phone %></td>
            <td><%= el.User.Profile.address %></td>
            <td><button><a href="">Buy Now</a></button></td>
          </tr>
        <% }) %>
      </table>
      
      <% } else { %>
        <p class="empty">Belum ada item ditampilkan di sini.</p>
      <% } %>
        
      <div class="actions">
        <a class="btn" href="/products">← Kembali ke Product List</a>
        <a class="btn" href="/wishlist/<%= data.id %>/buyNow">Buy Now</a>
      </div>
    </div>
  </body>
</html>
